export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Meal {
  name: string,
  calories: number,
  fat: number,
  carbs: number,
  protein: number,
  sodium: number,
  calcium: string,
  iron: string
}
